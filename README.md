A Task Manager application built with React + Vite that allows users to:

Add new tasks

Search tasks

Mark tasks as complete

Undo completed tasks

This project demonstrates the use of modern React Hooks including useContext, useRef, and useId.
